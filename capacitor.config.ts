import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.mastermimi.icnpj',
  appName: 'iCNPJ',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
